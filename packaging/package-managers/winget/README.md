# winget Template

Replace placeholders in these files after the Windows installer URL, sha256,
installer type, publisher name, and silent switches are final. Use the winget
tooling to generate or validate the final multi-file manifest.

Suggested package identifier: `Irodori.Table`.
