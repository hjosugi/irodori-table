name: irodori-sqlserver
# Irodori URL (ADO): server=tcp:localhost,51433;User Id=sa;Password=Irodori_Strong!23;TrustServerCertificate=true
# No auto-init; the connect check runs a self-contained VALUES query.
services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Irodori_Strong!23"
    ports:
      - "51433:1433"
